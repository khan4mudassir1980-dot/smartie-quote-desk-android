<resources>
    <!--
        Launch theme only. Everything after the first frame is drawn by
        SmartieTheme in Compose; the window is edge to edge with transparent
        system bars so the app can draw behind them and apply its own insets.
    -->
    <style name="Theme.SmartieQuoteDesk" parent="android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/smartie_paper</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowActionModeOverlay">true</item>
    </style>
</resources>
