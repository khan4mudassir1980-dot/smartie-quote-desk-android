{
  "name": "smartie-catalogue-import",
  "private": true,
  "type": "module",
  "description": "Derives the approved V8C4 catalogue and imports it into the staging Firebase project. Never writes to production.",
  "scripts": {
    "check": "node --check extract-v8c4.mjs && node --check export-production.mjs && node --check import-staging.mjs && node --check verify-staging.mjs"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0"
  }
}
